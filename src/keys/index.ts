// import { ClientKeys } from '../types'

// const keys: ClientKeys = {
//   token: process.env.BOT_TOKEN ?? 'nil'
// }

// if (Object.values(keys).includes('nil')) { throw new Error('Not all env variables are initialised!') }

// export default keys
